{
  "name": "@packages/feature-courses",
  "version": "1.0.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p ./tsconfig.lib.json && tsgo --noEmit -p ./tsconfig.spec.json",
    "test": "vitest run",
    "test:ci": "vitest run --hideSkippedTests --silent --no-color"
  },
  "dependencies": {
    "@packages/shared": "*"
  },
  "devDependencies": {}
}
